#include <bits/stdc++.h>
using namespace std;

int main(int argc, char const *argv[])
{
    int var = 50;
    int *p;
    p = &var;

    /*

    cout<<var<<endl; // outputs 50 (the values of var)
    cout<<endl;

    cout<<p<<endl;  // outputs memory address of var or memory location
    cout<<endl;

    cout<<&var<<endl; // outputs memory address of var or memory location 
    cout<<endl;

    cout<<*p<<endl;  // outputs 50 (the values of the varialble stored in the pointer p)
    cout<<endl;

    */

   /*

    var = var + 40;  // var = 90;
    var = *p +40;  // var = 90;
    *p = *p + 40;  // var = 90;

    // above 3 equation return the same result

    */

   


    return 0;
}