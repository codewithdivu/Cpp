#include <bits/stdc++.h>
using namespace std;

int maxOfArray(int arr[], int size)
{
    static int maxu = -1;
    if (size == 0)
    {
        return maxu;
    }

    if(arr[m])

}

int main(int argc, char const *argv[])
{
    int arr[] = {12, 54, 23, 63, 23, 112, 52, 78, 99, 54};
    cout << "maximum is " << maxOfArray(arr, 10);
    return 0;
}