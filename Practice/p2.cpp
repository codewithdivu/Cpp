#include <bits/stdc++.h>
using namespace std;

// permutation

void permutation(string &str, int i){
    
}

int main(int argc, char const *argv[])
{
    string str = "abc";
    permutation(str,0,str.length()-1);
    return 0;
}